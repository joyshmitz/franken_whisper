# Skill Loop Progress
# Skill: extreme-software-optimization (YOUTUBE orchestration layer; engine rounds 1-3 recorded in tests/artifacts/perf/.../RESULTS.md)
# Target: src/youtube/ (engine internals already at floor)
# Total Passes: 5
# Started: 2026-06-07T05:30Z

## Status: IN PROGRESS — Pass 3 of 5 (Passes 1-2 DONE)

## Context
Hotspot table: tests/artifacts/perf/20260607-youtube-baseline/HOTSPOTS.md.
Real single-video run (jNQXAC9IVRw, tiny.en): wall 12.46s = download 5.7s
(network-irreducible) + 3 metadata fetches (~1.6s resolve + 1.6s download
+ 1.7s render = ~4.9s, 2/3 redundant) + transcribe 0.58s + render 12ms.
Stub call-log proved 3 `yt-dlp -j` calls/video. Measurement infra:
FRANKEN_WHISPER_PERF_SPANS=1 emits yt.* spans; stub STUB_CALL_LOG counts
invocations; perf binary at $(cat /tmp/fw_perf_bin).
GOLDEN GATE: md/json output identical (metadata is the same, just fetched
once) — verify via the gated stub integration test (tests/youtube_integration.rs,
needs tiny.en) byte-comparing outputs before/after, AND a live re-run.
Discipline: yt-dlp network times vary run-to-run; the CALL COUNT (stub log)
is the load-robust metric for the metadata lever; wall is corroboration.

## Missions
1. Collapse 3 metadata fetches -> 1: resolve_videos does NOT fetch for Video/Ambiguous URLs (extract id locally via the URL parsing classify_url already does — add ytdlp::extract_video_id); the download worker does the single authoritative fetch_metadata and CARRIES the VideoMeta on the channel; transcribe_and_render consumes the carried meta (no re-fetch). Removes 2 round-trips/video AND the serial pre-pool fetch so download concurrency starts immediately. Golden: identical md/json.
2. Manifest write-volume: 7 save sites each rewrite the whole BTreeMap -> O(N^2) bytes for large playlists. Coalesce saves (per terminal transition, not every intermediate) WITHOUT losing crash-resumability. Measure write bytes for a synthetic 200-entry manifest.
3. Resolve/expand efficiency + dedup at scale; any remaining per-URL serial network; HashSet alloc; large-playlist memory.
4. Bug/omission hunt (user asked): cancellation correctness with VideoMeta now on the channel (no leak), carried-meta fallback when fetch fails, keep_audio interaction, manifest schema compat, unwrap/panic, id-extraction edge cases.
5. Fresh re-profile + landing: stub call-count must be 1 metadata/video + real wall; full gates; RESULTS.md; doc refresh; commit/push.

## Completed Passes

### Pass 1 (2026-06-07) — collapse 3 metadata fetches -> 1 ✅
- ADDED `ytdlp::extract_video_id(&str) -> Option<String>`: pure URL parse reusing
  the same grammar as `classify_url` (watch?v=, watch?v=X&list=Y -> v param,
  youtu.be/, shorts/, live/, embed/; mobile/music/nocookie hosts; scheme
  optional). Refactored `query_has_nonempty_param` onto a shared
  `query_param_value` helper. Unit tests: per-form + a coverage test asserting
  EVERY url classify_url calls Video/Ambiguous yields Some(id) (so the resolve
  fallback fetch is provably never hit for valid inputs).
- resolve_videos: Video/Ambiguous now build VideoRef from extract_video_id
  (title="", duration None) with NO fetch_metadata; single-fetch fallback only
  if extract_video_id returns None (defensive, unreachable per coverage test).
  Killed the serial pre-pool round-trip → download concurrency starts immediately.
- DownloadResult.outcome widened to Result<(PathBuf, VideoMeta), String>;
  download_one returns the meta it fetched (the ONE authoritative -j); the
  cancellation-persist path destructures `(audio_path, _meta)`.
- transcribe_and_render takes &VideoMeta (no re-fetch); dropped its info param
  and the old fallback. Resume path: a `Downloaded`-state video falls into the
  `_` partition arm → re-enters download_one → gets fresh meta, so there is NO
  genuine no-meta path (verified). yt.render_metadata span removed.
- MEASURE: stub `-j` calls 6 -> 2 (exactly 1/video); `-f` downloads 2 (unchanged).
- GOLDEN: md/json byte-identical before/after except inherent run-time fields
  (started timestamp, wall_ms/rtf, "transcribed in Xs" line). Same file sizes.
- GATES: cargo test --lib youtube:: = 116 passed; cargo test --test
  youtube_integration --test-threads=2 = 2 passed (incl. resume test);
  fmt --check clean; clippy --lib -D warnings exit 0. Files: src/youtube/ytdlp.rs,
  src/youtube/pipeline.rs.

### Pass 2 (2026-06-07) — reduce manifest write-volume (hotspot #4) ✅
- 7 save sites classified: (1) L304 bulk-init after upsert loop — single O(N)
  write, KEEP; (2) L384 cancel-persist Downloaded — rare cancel path, KEEP;
  (3) L411 intermediate Downloaded (post-download, pre-transcribe) — DROPPED;
  (4) L429 terminal Done — CRASH-CRITICAL, KEEP; (5) L434 Cancelled-in-transcribe
  — redundant (state still Pending, no terminal change), DROPPED; (6) L490
  record_failure Failed save — CRASH-CRITICAL, KEEP; (7) record_failure called
  from both download-fail (L392) + transcribe-fail (L439), single save.
- DROPPED the intermediate Downloaded save (was set_state+save before transcribe)
  and the redundant Cancelled save. SAFE because the partition logic re-enters
  download_one for ANY non-Done/non-Failed entry — a Downloaded entry is
  re-downloaded on resume regardless, so the intermediate save bought ZERO
  resume benefit (pure write amplification). Done + Failed remain durable
  immediately (crash-safety preserved); the contract permits re-downloading.
  Net: ~1 save/video terminal transition (was 2-3).
- MEASURE (new #[test] manifest_write_volume_halves_after_coalescing, K=200):
  old=24,069,528 B -> new=12,065,628 B, saved 12,003,900 B = 49.9% reduction.
- O(N²) NOTE documented in-code: even at 1 save/video the total is O(N²) bytes
  (each save full-rewrites the BTreeMap). At realistic N<500 (~200 B/entry) =
  tens of MB cumulative — modest. Future bead: swap to append-only journal
  (O(1) amortized, compacted on load) ONLY if N>~2000 ever matters. Did NOT
  build the journal (numbers don't demand it).
- GATES: cargo test --lib youtube::pipeline = 5 passed (incl. new byte-count
  test); cargo test --test youtube_integration --test-threads=2 = 2 passed
  (resume correctness proof green); fmt --check clean; clippy --lib -D warnings
  exit 0. Files: src/youtube/pipeline.rs only.
